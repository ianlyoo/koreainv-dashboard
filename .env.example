APP_KEY=
APP_SECRET=
URL_BASE=https://openapi.koreainvestment.com:9443
CANO=
ACNT_PRDT_CD=01
TRADE_MODE=live
PAPER_URL_BASE=https://openapivts.koreainvestment.com:29443
COOKIE_SECURE=false

# Optional read-only Toss proxy (keep real values in the private .env only)
TOSS_PROXY_SERVER_ENABLED=false
TOSS_PROXY_SERVER_TOKEN=
TOSS_PROXY_REMOTE_URL=
TOSS_PROXY_REMOTE_TOKEN=

# Optional SaveTicker insights: connect from dashboard Settings -> SaveTicker.
# Credentials are session-only by default; remember uses the OS credential store.
# Do not add SaveTicker email/password to this file or to a local .env.
